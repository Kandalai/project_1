name: project_1
description: RainSafe Navigator - Weather-intelligent navigation app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
 

  # Location & Maps
  geolocator: ^14.0.2
  flutter_map: ^8.2.2
  latlong2: ^0.9.0

  # Voice & Audio
  flutter_tts: ^4.2.5

  # HTTP & Storage
  http: ^1.1.0
  shared_preferences: ^2.2.2

  # UI Components
  cupertino_icons: ^1.0.2
  flutter_compass: ^0.8.1
  url_launcher: 6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# ✅ FIXED: Assets configuration
flutter:
  uses-material-design: true



  # Logo and Images
  assets:
    - assets/
    - assets/logo.png

  # If you want to add custom fonts (optional):
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700

dependency_overrides:
  url_launcher_android: 6.3.10